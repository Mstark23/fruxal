# Full US/CA Audit — March 29, 2026

**49 files changed** | +652 / -320 lines | 3 commits

---

## Issues Found & Fixed

### CRITICAL — Prescan Broken (Both US + CA)

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 1 | **Prescan chat shows "I'm having trouble"** | Model `claude-sonnet-4-5-20251029` doesn't exist — Anthropic returns 404 | Changed to `claude-sonnet-4-20250514` across 16 files | prescan-chat, client.ts, brief-generator, goal-suggester, comparison-generator, prescan-linker, task-generator, parse-doc, playbooks, public-company, drill, diagnostic-test, tier3 diagnostic |
| 2 | **Prescan form: infinite spinner on error** | `isLoading` initialized as `true`, never set `false` on error; no error message shown | Fixed error handling with try/catch/finally, added error display | `app/v2/prescan/page.tsx` |
| 3 | **Prescan results: staggered reveal broken** | 5 `setTimeout` calls — first `return` made rest unreachable (dead code) | Collected all timers in array, proper cleanup on unmount | `app/v2/prescan/results/[id]/page.tsx` |

### CRITICAL — Diagnostic Prompts Wrong Country (All Tiers)

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 4 | **CA Solo diagnostic gets US tax forms** | `buildQualityBar("solo", "US")`, `buildSolutionMatrix(..., "US")`, `buildDiagnosticSchema("solo", 5, "US")` hardcoded in CA path | Changed all 3 to `"CA"` | `lib/ai/prompts/diagnostic/solo.ts` |
| 5 | **CA Business diagnostic gets US tax forms** | Same 3 hardcoded `"US"` in CA path | Changed all 3 to `"CA"` | `lib/ai/prompts/diagnostic/business.ts` |
| 6 | **CA Enterprise diagnostic gets US tax forms** | Same 3 hardcoded `"US"` in CA path | Changed all 3 to `"CA"` | `lib/ai/prompts/diagnostic/enterprise.ts` |
| 7 | **US Solo diagnostic crashes (JSON parse)** | `userPrompt` missing JSON schema — Claude returns text, `JSON.parse` fails | Added profile fields + `buildDiagnosticSchema()` call | `solo.ts` |
| 8 | **US Business diagnostic crashes** | Same: `userPrompt` truncated, missing schema | Added full profile fields + schema | `business.ts` |
| 9 | **US Enterprise diagnostic missing fields** | `userPrompt` truncated, missing profile fields + schema | Added full profile + schema | `enterprise.ts` |

### CRITICAL — US Users Get Quebec Data

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 10 | **10+ routes default `province \|\| "QC"`** | Every fallback assumes Canada/Quebec | Changed to `""` — empty string returns no results instead of wrong results | obligations/sync, smart-prescan, intelligence, prescan v1, leaks, task-generator, intelligence.ts, prescan-chat v3, oblig2 |
| 11 | **find-user-leaks query misses US federal leaks** | `.eq("province", province)` doesn't match "ALL" | Changed to `.in("province", [province, "ALL"])` | `lib/find-user-leaks.ts` |
| 12 | **Prescan obligations query misses federal** | `.eq("province", province)` exact match | Fetch all, filter by `applies_to_provinces` array (empty = federal) | `prescan/analyze/route.ts` |
| 13 | **Prescan programs query misses federal** | `.contains("provinces", [province])` doesn't match empty array | Fetch all, filter (empty = federal) | `prescan/analyze/route.ts` |
| 14 | **No US data in database** | Tables only had CA provinces | Created 3 SQL seed files: 25 obligation rules, 28 leak detectors, 22 programs | `sql/us-*.sql` |

### CRITICAL — Contingency Model Not Implemented

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 15 | **Features locked behind paid plans** | `plan-gate.ts` restricted diagnostic, PDF, chat to paid tiers | All features now include `"free"` tier | `lib/plan-gate.ts` |
| 16 | **3-leak limit for free users** | `FREE_LEAK_LIMIT = 3` in leaks API | Removed limit, all leaks visible | `app/api/v2/leaks/list/route.ts` |
| 17 | **Leaks blurred for free users** | `applyPaywall()` replaced leak titles with lock icons | Function now returns all leaks unmodified | `services/billing/stripe.ts` |
| 18 | **Chat blocked by 402 paywall** | ChatInterface checked for 402 status, showed lock UI | Removed paywall check, `paywalled` always false | `components/v2/ChatInterface.tsx` |
| 19 | **Upgrade modal shows pricing** | UpgradePrompt showed "$49/mo", "$149/mo", link to checkout | Replaced with contingency CTA: "Book a Free Recovery Call", 12% model | `components/v2/UpgradePrompt.tsx` |
| 20 | **Translations reference paid plans** | "Get Full Report — $47 CAD", "AI Advisor — $79/mo CAD" | Updated to contingency copy (EN + FR) | `lib/i18n/translations.ts` |

### HIGH — Country Detection / Persistence

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 21 | **Country not saved during onboarding** | `country` field missing from DB payload | Added `country` to upsert | `app/api/v2/onboarding/route.ts` |
| 22 | **exports_goods not saved** | Collected in UI but never in POST payload | Added to upsert | `app/api/v2/onboarding/route.ts` |
| 23 | **Diagnostic intake doesn't save country** | `country` + `province` missing from update payload | Added both fields | `app/api/v2/diagnostic/intake/route.ts` |
| 24 | **Dashboard defaults to QC/CA** | `{ province: "QC", country: "CA" }` in useState | Changed to `{ province: "", country: "" }` | solo, business, dashboard API |
| 25 | **No USD checkout** | All Stripe sessions hardcoded `currency: "cad"` | Detects country, sets `"usd"` or `"cad"` with appropriate pricing | `app/api/v2/checkout/route.ts` |

### HIGH — US/CA Content Mismatches

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 26 | **Prescan: no US insights** | All insights hardcoded for CA (GST, Law 25, provincial rates) | Added US insights (S-corp/FICA, CCPA, sales tax nexus, R&D credit, QBI) | `prescan/analyze/route.ts` |
| 27 | **US says "accountant" instead of "CPA"** | Hardcoded "Our accountant" in US code paths | Changed to "Our CPA" for US flows | solo, business dashboards, prescan results |
| 28 | **"outside Canada" shown to US users** | Hardcoded label in prescan form | Ternary: "outside the United States" for US | `app/v2/prescan/page.tsx` |
| 29 | **Province insights only for CA** | Hardcoded SK/AB/NL tax facts | Added `getUSStateInsight()` for US states | `app/v2/prescan/page.tsx` |
| 30 | **US prescan chat prompt not wired** | `system-us.ts` existed but was never imported | Import + country param in `buildSystemPrompt()` | `app/api/v3/prescan-chat/route.ts` |

### HIGH — Onboarding Issues

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 31 | **QST field shows for US users** | `province === "QC"` without country check | Added `&& data.country !== "US"` guard | `app/v2/onboarding/page.tsx` |
| 32 | **GST label uses brittle cookie check** | `document.cookie.includes("fruxal_country=US")` | Changed to `data.country === "US"` | `app/v2/onboarding/page.tsx` |
| 33 | **Silent failure on businesses table** | `.catch(() => {})` swallows errors | Now logs warning message | `app/api/v2/onboarding/route.ts` |

### MEDIUM — UI/Localization

| # | Issue | Root Cause | Fix | Files |
|---|---|---|---|---|
| 34 | **Leaks PDF French-only** | All text hardcoded French | Added `?lang=` param, all text bilingual EN/FR | `app/api/v2/leaks/report/route.ts` |
| 35 | **Enterprise dashboard dead code** | Duplicate `profile.country === "US"` conditional — second branch unreachable | Fixed ternary | `enterprise/page.tsx` |
| 36 | **French accent typos** | "a risque", "Resoudre", "jusqu a", "recupere" | Fixed: "a risque", "Resoudre", "jusqu'a", "recupere" | `solo/page.tsx` |
| 37 | **V2AppShell English-only nav** | "Dashboard", "AI Advisor" hardcoded | Added FR: "Tableau de bord", "Conseiller IA" | `components/v2/V2AppShell.tsx` |
| 38 | **Prescan results: "10 provinces"** | Hardcoded for Canada | Shows "50 states" for US users | `prescan/results/[id]/page.tsx` |
| 39 | **UpgradePrompt English-only** | No FR translations, no US/CA awareness | Full bilingual + 3 step variants (EN-CA, EN-US, FR) | `components/v2/UpgradePrompt.tsx` |
| 40 | **Leaks partner matching hardcoded QC** | `getSmartPartners({ province: "QC" })` | Now reads from query param | `app/api/v2/leaks/route.ts` |

---

## New Files Created

| File | Purpose |
|---|---|
| `sql/us-obligation-rules.sql` | 25 US obligation rules (IRS, state filing deadlines, FUTA, workers comp, BOI, sales tax) |
| `sql/us-leak-detectors.sql` | 28 US leak detection patterns (SE tax, S-corp, QBI, R&D credit, Section 179, CCPA, etc.) |
| `sql/us-programs.sql` | 22 US government programs (SBA 7a/504/Microloan, SBIR, WOTC, R&D credit, state programs) |

**Run these in Supabase SQL editor to populate US data.**

---

## Commits

```
056df3e fix: prescan chat crash — model claude-sonnet-4-5-20251029 does not exist
747a775 fix: eliminate all QC defaults, fix data pipeline for US users
0f41411 fix: full US/CA audit — prescan, diagnostics, contingency model, seed data
```
