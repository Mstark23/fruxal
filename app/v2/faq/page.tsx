// =============================================================================
// app/v2/faq/page.tsx — FAQ inside the authenticated v2 area
// Uses the same content as the public /faq page, styled for the dashboard shell
// =============================================================================
export { default } from "@/app/faq/page";
